[mypy]
# don’t barf on third-party stubs, we just want to type-check src/
ignore_missing_imports = True
# avoid scanning virtual-env and build artifacts
exclude = (^|/)(\.venv|venv|build|dist)/
# only check your code, not environment
files = src/
